import { MonthContainer } from "../MonthContainer";
import { render, screen } from "@testing-library/react";

describe("MonthContainer", () => {
  it("should be rendered", () => {
    render(<MonthContainer />);
    //expect(screen.getByText);
  });
});
